# WireGuard Client Configuration Template
# Copy to /etc/wireguard/wg0.conf and fill in the placeholders

[Interface]
PrivateKey = <CLIENT_PRIVATE_KEY>
Address = <CLIENT_IP>/24  # 10.100.0.10 for Agent1, 10.100.0.11 for Agent2, 10.100.0.20 for laptop

[Peer]
PublicKey = <SERVER_PUBLIC_KEY>
Endpoint = <ALIYUN_SG_PUBLIC_IP>:51820
AllowedIPs = 10.100.0.0/24  # Only route VPN traffic through tunnel
PersistentKeepalive = 25     # Keep connection alive through NAT
